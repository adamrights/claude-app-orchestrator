# Expected output

When the orchestrator builds [`blueprint.yaml`](./blueprint.yaml) into
`./app/`, this is the file tree it produces. The `app/` subdirectory is
gitignored (see `examples/built/.gitignore`) so reference builds stay
source-only in version control — this document describes what's there
without checking it in.

Each path is annotated with the specialist or skill responsible for it.

```
app/
├── prisma/
│   ├── schema.prisma                    # Models from blueprint: User, Ticket, Comment, Tag, TicketTag — written by Project Initializer
│   └── migrations/
│       └── <timestamp>_init/            # Initial migration generated by Migration Specialist
│           └── migration.sql
├── src/
│   ├── app/
│   │   ├── layout.tsx                   # Root layout with NextAuth SessionProvider + Tailwind globals
│   │   ├── page.tsx                     # Landing/redirect to /tickets when authed
│   │   ├── api/
│   │   │   ├── auth/
│   │   │   │   └── [...nextauth]/
│   │   │   │       └── route.ts         # NextAuth GitHub OAuth handler — auth feature
│   │   │   └── cron/
│   │   │       └── sla-check/
│   │   │           └── route.ts         # Cron entrypoint that invokes jobs/sla-checker.ts
│   │   ├── tickets/
│   │   │   ├── page.tsx                 # Ticket list (RSC) — Data Table Builder output
│   │   │   ├── actions.ts               # Server Actions: createTicket, updateStatus, assignAgent, deleteTicket — Zod-validated, RBAC-checked
│   │   │   ├── ticket-row-actions.tsx   # 'use client' — per-row dropdown (assign, change status)
│   │   │   ├── [id]/
│   │   │   │   ├── page.tsx             # Ticket detail (RSC) — RSC Architect output
│   │   │   │   ├── status-control.tsx   # 'use client' — status state-machine dropdown
│   │   │   │   ├── comment-thread.tsx   # RSC list of comments with internal-note filter
│   │   │   │   └── comment-form.tsx     # 'use client' — composer with optimistic insert + internal-note toggle
│   │   │   └── new/
│   │   │       ├── page.tsx             # Multi-step create form shell
│   │   │       ├── form-step-content.tsx   # Step 1: title + description
│   │   │       ├── form-step-meta.tsx      # Step 2: priority + tags
│   │   │       └── form-step-review.tsx    # Step 3: review + submit
│   │   ├── agent/
│   │   │   └── dashboard/
│   │   │       ├── page.tsx             # Metrics + queue (RSC) — Dashboard Builder output
│   │   │       ├── kpi-cards.tsx        # Open / in-progress / overdue counters
│   │   │       ├── status-chart.tsx     # 'use client' — Recharts status breakdown
│   │   │       └── my-queue-table.tsx   # Personal-queue table extract
│   │   └── admin/
│   │       └── users/
│   │           ├── page.tsx             # User list with role filter — Admin Panel Builder output
│   │           ├── actions.ts           # Server Actions: changeRole — admin-only
│   │           └── role-dialog.tsx      # 'use client' — promote/demote modal
│   ├── components/
│   │   └── ui/                          # Design-system primitives — Design System Builder output (shared wave)
│   │       ├── button.tsx
│   │       ├── dialog.tsx
│   │       ├── input.tsx
│   │       ├── form.tsx                 # react-hook-form + Zod resolver wrapper
│   │       ├── badge.tsx                # Used for status/priority/tag pills
│   │       ├── card.tsx
│   │       ├── table.tsx                # Headless table primitives consumed by Data Table Builder
│   │       ├── tooltip.tsx
│   │       └── dropdown-menu.tsx
│   ├── lib/
│   │   ├── prisma.ts                    # Prisma client singleton
│   │   ├── auth.ts                      # NextAuth config (GitHub provider + role-on-signup callback)
│   │   ├── rbac.ts                      # Permission checks + role utilities — RBAC Specialist output
│   │   ├── permissions.ts               # Permission enum + role→permissions map — RBAC Specialist output
│   │   ├── search.ts                    # Postgres FTS query helpers (tsvector + GIN) — uses search.md skill
│   │   ├── email.ts                     # Resend client wrapper — Integration Specialist output
│   │   ├── email-templates/             # Typed email templates (assignment, reply, resolved, sla-breach)
│   │   │   ├── ticket-assigned.tsx
│   │   │   ├── new-comment.tsx
│   │   │   ├── ticket-resolved.tsx
│   │   │   └── sla-breach.tsx
│   │   ├── sla.ts                       # Compute slaDeadline from priority at ticket-create time
│   │   └── status-machine.ts            # Allowed status transitions — uses state-machines skill
│   ├── jobs/
│   │   └── sla-checker.ts               # Cron handler — Background Jobs Specialist output
│   └── middleware.ts                    # Role-gated route middleware (uses lib/rbac.ts)
├── tests/
│   ├── unit/
│   │   ├── status-machine.test.ts       # State-machine transitions
│   │   └── rbac.test.ts                 # Permission resolution per role
│   ├── component/
│   │   ├── comment-form.test.tsx        # Optimistic insert behavior
│   │   └── ticket-list-table.test.tsx   # Sort/filter/paginate
│   └── e2e/
│       ├── ticket-create.spec.ts        # Customer files a ticket end-to-end
│       ├── agent-triage.spec.ts         # Agent assigns + changes status
│       └── rbac-enforcement.spec.ts     # Customer cannot read other customers' tickets
├── BUILD_REPORT.md                      # Generated by orchestrator Phase 4: env vars, specialist invocations, commit map
├── README.md                            # Project-level overview + getting started (CLAUDE.md-style)
├── .env.example                         # DATABASE_URL, NEXTAUTH_SECRET, GITHUB_ID, GITHUB_SECRET, RESEND_API_KEY
├── package.json                         # Next.js, Prisma, NextAuth, react-hook-form, zod, resend, recharts, @radix-ui/*, tailwindcss
├── tsconfig.json
├── tailwind.config.ts
├── postcss.config.mjs
├── next.config.mjs
└── .gitignore
```

## Commit map

The orchestrator commits per feature (and per shared-primitive). Expected
commit list, in dependency order:

1. `chore: scaffold next.js + prisma + tailwind`
2. `feat(shared): design-system primitives`
3. `feat(auth): github oauth + role-on-signup`
4. `feat(rbac-foundation): permissions + middleware`
5. `feat(ticket-crud-api): server actions for tickets`
6. `feat(ticket-list-table): searchable filterable table`
7. `feat(ticket-detail-page): RSC ticket detail`
8. `feat(comment-thread): comments with optimistic updates`
9. `feat(ticket-create): multi-step new-ticket form`
10. `feat(ticket-search): postgres full-text search`
11. `feat(agent-dashboard): KPIs + status chart + my queue`
12. `feat(email-notifications): resend integration + templates`
13. `feat(sla-timer-job): cron handler for SLA breaches`
14. `feat(user-management): admin panel for roles`
15. `test: component + integration coverage`
