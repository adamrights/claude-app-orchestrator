// This file is regenerated by the orchestrator from the blueprint's `models` section.
// The base content here is just enough to get `prisma generate` working.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
