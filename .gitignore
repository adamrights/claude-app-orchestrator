.DS_Store
node_modules/
*.swp
*.swo
*~

# Ralph loop runtime state (template is tracked, state/logs are not)
.ralph/state.json
.ralph/logs/

# Claude Code's local state under .claude/ — commands/ are tracked source; the rest is per-machine.
.claude/agent-memory/
.claude/agents/
